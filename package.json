{
  "name": "pastel",
  "scripts": {
    "install:develop": "npm i --production=false",
    "install:production": "npm i --production",
    "watch:scripts": "tsc-watch --onSuccess 'npm start'",
    "build:scripts": "tsc",
    "lint": "ts-standard --fix --verbose | snazzy",
    "start": "node dist/index.js"
  },
  "ts-standard": {
    "ignore": [
      "dist"
    ]
  },
  "dependencies": {
    "fastify": "^3.20.1"
  },
  "devDependencies": {
    "@types/node": "^16.4.13",
    "snazzy": "^9.0.0",
    "ts-standard": "^10.0.0",
    "tsc-watch": "^4.4.0",
    "typescript": "^4.3.5"
  }
}
